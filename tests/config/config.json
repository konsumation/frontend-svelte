{
  "database": {
    "file": "build/db"
  },
  "auth": {
    "users": {
      "user1": {
        "password": "secret",
        "entitlements": ["konsum"]
      }
    },
    "jwt": {
      "options": {
        "expiresIn": "1h"
      },
      "public": "${document('demo.rsa.pub')}",
      "private": "${document('demo.rsa')}"
    }
  },
  "http": {
    "port": 12345
  }
}
